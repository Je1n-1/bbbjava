<?xml version="1.0" encoding="UTF-8"?>
<!--
    faces-config.xml — Configuração do Jakarta Faces (JSF)
    Define:
      - Regras de navegação entre views JSF
      - Configurações de internacionalização (pt-BR)
      - Managed Beans (alternativa às anotações, para compatibilidade)
-->
<faces-config xmlns="https://jakarta.ee/xml/ns/jakartaee"
              xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
              xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
                  https://jakarta.ee/xml/ns/jakartaee/web-facesconfig_4_0.xsd"
              version="4.0">

    <!-- ============================================================
         APLICAÇÃO — Configurações Globais
         ============================================================ -->
    <application>
        <!-- Idioma padrão: Português Brasileiro -->
        <locale-config>
            <default-locale>pt_BR</default-locale>
            <supported-locale>pt_BR</supported-locale>
        </locale-config>
    </application>

    <!-- ============================================================
         REGRAS DE NAVEGAÇÃO JSF
         Definem os outcomes (retornos dos métodos dos Managed Beans)
         e para qual view cada um redireciona.
         ============================================================ -->

    <!-- Após cadastro bem-sucedido: vai para a listagem -->
    <navigation-rule>
        <from-view-id>/faces/cadastro.xhtml</from-view-id>
        <navigation-case>
            <from-outcome>listar</from-outcome>
            <to-view-id>/faces/listar.xhtml</to-view-id>
            <redirect/>
        </navigation-case>
    </navigation-rule>

    <!-- ============================================================
         MANAGED BEAN (alternativa à anotação @ManagedBean)
         Mantido aqui para compatibilidade com diferentes servidores.
         ============================================================ -->
    <managed-bean>
        <managed-bean-name>livroBean</managed-bean-name>
        <managed-bean-class>controller.LivroBean</managed-bean-class>
        <managed-bean-scope>session</managed-bean-scope>
    </managed-bean>

</faces-config>
